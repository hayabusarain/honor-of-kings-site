# パッチノート反映の手順書

公式アップデート情報を `src/data/patches.json` と `src/data/patch_meta.json` に反映するときは、毎回この手順に従う。
毎回同じ形になることが目的なので、思いつきで書式を変えない。

---

## 1. 公式原文を取得する

### 1-1. 新しい記事を探す

お知らせの一覧は、公式ページを開いた中から取る。**curl では取れない**（2026-09-18 に確認。
下の一覧APIを curl で叩くと、正しい条件でも `info_content` が空で返る。Cookie か
セッションを見ているとみられる。本文API（1-2）は curl のままで取れる）。

```bash
npm run check:news   # 日英の最新10件と content_id、反映済みかどうかを出す
```

`scripts/check_official_news.mjs` が公式の一覧ページ（日本語 `https://www.honorofkings.com/jp/news-list.html`、
英語 `https://www.honorofkings.com/global-en/news-list.html`）を開き、記事のタイトル・日付・`content_id` を読む。
日本語の記事は `patches.json` と月日（「9月10日」）で突き合わせ、反映済みかどうかも出す。
公式はタイトルの付け方を回によって変える（例「7月30日S15.aミッドシーズンバージョンアップデートのお知らせ」）ため、
全文一致では照合しない。英語の記事は日本語と別タイトルなので `[ 英語 ]` とだけ出す。

一覧APIを直接叩くときは、公式ページを開いた `page.evaluate()` の中から次を POST する。

```
https://hok-sg-community.playerinfinite.com/api/gpts.information_feeds_svr.InformationFeedsSvr/GetContentByLabel
{"language":["ja"],"gameid":"9","offset":0,"get_num":10,
 "ext_info_type_list":[0,1,2],"secondary_label_id":"1158","content_class":0,
 "primary_label_id":"785","third_label_id":0}
```

**ラベルIDは公式の都合で変わる。** 2026-09-17 時点で `primary_label_id` は 671 → 785、
`secondary_label_id` は 0 → "1158"、`content_class` は 1 → 0 に変わっていた。
旧条件はエラーにならず、0件で返るだけなので「新着なし」と取り違えやすい。
一覧が急に0件になったら、まず条件が変わったことを疑い、公式ページ自身の通信を見て
今の条件を読み取る。

```bash
npm run check:news -- --sniff   # 一覧ページが投げているリクエストと応答を出す
```

### 1-2. 本文を取得する

公式サイト（`https://www.honorofkings.com/jp/news-detail.html?content_id=＜ID＞`）は中身が空の SPA なので、
**HTML を取得しても本文は入っていない**。本文は CMS の API から取る（curl で取れる。2026-09-18 に確認）。

```bash
CONTENT_ID="＜URLのcontent_idをそのまま＞"
curl -s -X POST \
  "https://hok-sg-community.playerinfinite.com/api/gpts.information_feeds_svr.InformationFeedsSvr/GetContentInfoById" \
  -H "Content-Type: application/json" \
  -e "https://www.honorofkings.com/" \
  -d "{\"content_id\":\"$CONTENT_ID\",\"lang\":\"ja\"}" \
  -o news.json
```

- 本文は `data.content` に HTML で入っている。タグを落としてテキスト化してから読む。
- `data.title` が記事タイトル、`data.pub_timestamp` が公開時刻（秒）。
- `content_id` は記事ページの URL にそのまま入っている（`d32a6c5c…` のような英数字。数字だけとは限らない）。
- **`content_id` は言語ごとに別物**。日本語ページの ID で英語版は取れない。
  英語版の本文が必要なときは、英語ページの URL を別途もらう。
- ただし後述のとおり `description_en` は公式英文の転載ではなく独自に書くため、
  **英語版の記事が無くても反映作業は完結する**。

## 2. 書き込む先とスキーマ

### `src/data/patches.json`（変更1件＝1オブジェクト）

```jsonc
{
  "version": "8月13日アップデートのお知らせ",   // 公式の記事タイトルをそのまま
  "version_en": "August 13 Update",
  "change_type": "buff",                      // 下の分類ルールを参照
  "hero_name": "ルアンナ",
  "hero_name_en": "Luara",
  "is_hero": true,
  "hero_id": "547",                           // is_hero:true のときのみ。文字列
  "id": "patch_8_13_1",
  "description": "…",                         // 日本語本文（Markdown）
  "description_en": "…"                       // 英語本文（Markdown）
}
```

### `src/data/patch_meta.json`（バージョン1回＝1オブジェクト）

```jsonc
{
  "version": "8月13日アップデートのお知らせ",
  "change_type": "adjust",
  "hero_name": "Server Update 8/13",
  "hero_name_en": "Server Update 8/13",
  "summary": "…",                 // 日本語1行
  "details": ["…", "…"],          // 5行前後の箇条書き
  "id": "patch_meta_8_13",
  "prediction_ja": "…",           // メタ分析
  "prediction_en": "…",
  "created_at": "2026-08-13T00:00:00.000Z",
  "version_en": "August 13 Update"
}
```

## 2-2. 書いたあとに数値を突き合わせる（必須）

本文は公式の告知を読んで手で書き起こす。写し間違いは読み返しでは見つからない。
書き終えたら、本文の数値が原文に実在するかを機械で確かめる。

```bash
npm run verify:patch -- --version "<patches.json の version と同じ文字列>"   --source <原文をテキスト化したファイル>   --derived 270,12.6      # こちらが計算して出した数があれば並べる
```

- 見るのは**日本語本文だけ**。英語は「6枚」を six blades と書くように語で表す箇所があり、
  数の一致では測れない（実際、過去分88件のうち24件が語と数字の違いで食い違う）。
- 公式統計（`hero_stats_camp.json`）と実測ステータス（`hero_base_stats.json`）の値、
  および 0〜30 の小さい整数（件数・順位・箇条書きの数）は通す。
- 落ちたら、その数値の出どころをもう一度原文で探す。見つからなければ本文から消す。

2026-09-23 に運営者から「内容が正しいか毎回確認しているのか」と聞かれて作った。
それまでは目視だけで、書いたあとの突き合わせをしていなかった。

## 3. 分類ルール（`change_type`）

| 種類 | 使える値 | 補足 |
|---|---|---|
| ヒーロー項目（`is_hero: true`） | **`buff` / `nerf` のみ** | `adjust` は使わない。強化と弱体が混在する場合は、実戦での正味の影響で決める |
| システム項目（`is_hero: false`） | `adjust` / `new` | 不具合修正・仕様変更は `adjust`、新要素やイベントは `new` |

公式が「数値調整」と書いていても、内容が弱体なら `nerf` にする。公式の見出しではなく中身で判断する。

## 4. `id` の命名規則

```
ヒーロー項目   : patch_<月>_<日>_<連番>          例) patch_8_13_1
システム項目   : patch_<月>_<日>_system_<連番>   例) patch_8_13_system_1
patch_meta    : patch_meta_<月>_<日>            例) patch_meta_8_13
```

配列に追加する順序は **システム項目 → ヒーロー項目（buff を先、nerf を後）**。

## 5. 本文の書式

日本語・英語とも同じ構造にする。

```
**ヒーロー名 — 何が変わったかの一言**

**スキル1「スキル名」**
・クールダウン 12.5秒 → 10秒（スキルLv.ごとに-0.4秒）
・基本ダメージ 40（+8/Lv・物理攻撃の20%） → 30（+6/Lv・物理攻撃の12%）

**この変更の意味**
（独自解説。2〜5文）
```

- 変更点は必ず `・` の箇条書き1行ずつ。数値は「**変更前 → 変更後**」の形で並べる。
- 末尾の解説の見出しは、ヒーロー項目が `**この変更の意味**` / `**What this means**`、
  イベント項目が `**注目ポイント**` / `**Worth your time**`。
- 解説は「強くなりました」で終わらせない。**読者が立ち回りや購入判断をどう変えるべきか**まで書く。
- スキル名・数値は公式原文から取る。**原文に無い数値を推測で書かない。**

### 英語（`description_en`）

- 日本語の直訳にしない。英語として自然な文章にする。
- 構造（見出し・箇条書き・順序）は日本語と一致させる。数値が食い違っていないか必ず突き合わせる。
- ヒーロー名は `src/data/hok_heroes.json` の `name_en` を使う（勝手な訳語を作らない）。

## 6. 日本語の文体

`~/.claude/CLAUDE.md` の「自然な日本語のルール」に従う。特に効くのは次の3つ。

- 代名詞（彼・彼女）で主語をぼかさず、ヒーロー名を書く。
- 名詞を動詞化しない（「スタックする」ではなく「スタックを獲得する」）。
- 誇張しない。「最強」「圧倒的」「絶対」は使わない。

## 7. イベント・スキンの扱い

`その他のイベント`（`patch_*_system_*` / `change_type: "new"`）にまとめる。

- スキンごとに **価格・販売期間・入手条件** を必ず書く。割引がある場合は割引後価格も。
- 期間は公式表記の UTC を残す（例: `8月14日〜8月20日（UTC±0）`）。
- 末尾の `**注目ポイント**` に、買い時や損得の判断材料を書く。所持状況で価値が変わるものはその旨を明記する。

## 8. ヒーローのスキルデータ（`src/data/skills/*.json`）の更新

**パッチノートの数値をスクリプトで流し込まないこと。** ゲーム内のスクリーンショットから書き起こす。

理由は3つある。

1. **説明文には「ゲームが計算した表示値」が埋まっている。**
   例: `1回につき 74（40+物理攻撃の20%）の物理ダメージ` の `74` は、特定の物理攻撃値を前提に
   ゲームが算出した数字。パッチノートは `40（20%）→ 30（12%）` としか書かないので、
   **新しい表示値はパッチノートからは決まらない**。逆算して埋めると公式と違う数字が載る。
2. **仕様変更は数値置換では直せない。** 「斬断速度はレベルに応じて増加する」のような、
   仕組みそのものが変わった記述は、文章を書き直さないと嘘が残る。
3. **パッチノートに載らない波及がある。** 変更された値を別のスキルの説明文が参照している場合、
   そのスキルはパッチに名前が出てこないまま古くなる。
   （実例: 8月13日の啓はパッシブのみ変更だが、スキル1〜3の説明文が同じ回復値を参照していた）

### 手順

1. パッチノートから **変更対象のヒーローとスキル** を一覧化する。
2. 同じヒーローの**他のスキル**に、変更された値や仕組みへの言及がないか検索する（波及チェック）。
3. 1と2をまとめた撮影リストを作る（例: `PATCH_8_13_SHOTLIST.md`）。
4. ゲーム内で該当スキルを開いてスクリーンショットを撮り、**画面の表記どおりに**書き起こす。
5. `description` / `cooldown_text` / `table.rows` の3つすべてを更新する。表だけ直して説明文が古い、という取りこぼしが起きやすい。
6. `src/data/skills/en.json` 側も同じスキルを更新する。
7. 書き起こせなかったヒーローが残ったら、`src/data/data_freshness.json` の `skillData.pendingHeroIds` に
   そのヒーローID（`src/data/hok_heroes.json` の `id`）を入れる。そのページにだけ「反映待ち」の注記が出る。
   全員分が済んだら `[]` に戻す。

### 日本語のスクリーンショットから英語版も更新できるか

**数値はできる。文章はできない。**
ダメージ表・クールダウン・倍率は言語に依存しないので、日本語画面の数字をそのまま `en.json` に入れてよい。
一方 `description` は、仕様変更があったスキルだと英語の公式文が手元にない。既存の英文に合わせてこちらで
書き起こすことになるため、**公式の英語表記と一字一句同じである保証はない**。

そのため、**仕様変更があったヒーローはゲームの表示言語を英語に切り替えて撮り直す**。
数値だけの調整なら日本語画面だけで足りる。英語画面から起こすときの注意は2つ。

- 英語画面はアイコンで代用している箇所がある（`60 [移動速度アイコン]`）。既存データの流儀に合わせて
  英単語に開く（`60 Movement Speed`）。`[icon]` のまま入れると読者に意味が伝わらない。
- ダメージ表の行名も英語版の表記に合わせる（例: `Fire Rain Damage` を勝手に `AoE Damage` に変えない）。

英語画面を撮れなかった場合は、その旨をコミットメッセージに残し、後から突き合わせられるようにしておく。

### 自動化してよい範囲

`table.rows` と `cooldown_text` は、パッチノートが基準値と増分（例: `30（スキルLv.ごとに+6）`）を
両方示しているため機械的に計算できる。ただし**書き換える前に「変更前の値」が既存データに実在するか照合し、
一致しない場合はスキップして報告する**こと。想定と違う箇所を書き換える事故を防ぐ。
`description` は必ず人が確認する。

## 9. 反映後の検証（必須）

```bash
npm run audit    # i18nキー整合・EN側への日本語混入・画像参照切れ
npm run build    # 0 エラーであること
```

加えて、次を目視で確認する。

- **版ページ `/ja/patches/<created_at の YYYY-MM-DD>` が 200 で開くか。**
  `patch_meta.json` の `created_at` からURLを作っているので、書き忘れると
  その版のページが1本まるごと生成されない（`npm run audit` の検査20が拾う）。
  同じ日付の版が2つあっても片方が消える。

- `patches.json` と `patch_meta.json` の両方に同じ `version` / `version_en` が入っているか
- 追加したヒーローの `hero_id` が `src/data/hok_heroes.json` に実在するか
- `/ja/patches` と `/en/patches` の両方で、追加分が表示され英語ページに日本語が残っていないか

## チェックリスト

- [ ] 公式原文を API から取得し、全文を読んだ
- [ ] ヒーロー項目の `change_type` は `buff` / `nerf` のみになっている
- [ ] `hero_id` が実在するヒーローと一致している
- [ ] `id` の命名規則に従っている
- [ ] 日英で数値・構造が一致している
- [ ] 各項目の末尾に独自解説がある
- [ ] イベント項目に価格・期間・入手条件が入っている
- [ ] `patch_meta.json` にそのバージョンの要約とメタ分析を追加した
- [ ] `patch_meta.json` の `created_at` を書いた（版ページのURLになる）
- [ ] `/ja/patches/<日付>` が 200 で開き、その版の項目だけが出ている
- [ ] `npm run audit` と `npm run build` が通った
