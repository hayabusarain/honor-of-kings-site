I'm recalling the critical instructions: avoid `cat` for file operations, prefer `grep_search`, and abstain from general-purpose commands where specific tools exist. I've identified that the `src/app/[locale]/heroes/[id]/page.tsx` file includes `forms` logic. Now, I need to understand the structure of `jaSkills[heroId]` for multi-form heroes. I'll search for 'hero_082' (Pei/Tiger) in the transcript to examine a multi-form hero's data structure, making a targeted approach to glean the relevant information.