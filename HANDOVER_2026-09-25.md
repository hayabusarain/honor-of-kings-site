# 引き継ぎ 2026-09-25〜26（スマホ改善61件と新機能）

運営者の指示は「おすすめ順で最後まで進める。確認は要らない」（push も含む）。
2026-09-26 の時点で、61件のレビュー指摘は下の「残した課題」以外すべて反映し、push した。

## 1. 入ったもの

- 不具合: React #418（並べ替えと価格表示に言語を渡す）、フッターの年、SW が RSC を溜めない
- 速さ: 日本語ページは端末のフォント（/ja/heroes の固まる時間 12.5秒 → 2.2秒、転送 4.3MB → 1.3MB）、
  Noto の宣言を1組に（描画を止める CSS 182KB → 83KB）、ヒーロー詳細の JSON をサーバーから渡す（詳細部品 98.9KB → 57.4KB）
- スマホ: 入力欄16px、固定帯、ヒーロー一覧のプルダウン、Tier表の格子（18.6 → 7.5画面）、ステータス表の1件2行、
  装備一覧（29.1 → 8.1画面）、アルカナ計算機の集計帯と44pxボタン、パッチの目次、ガイドの目次と畳み、
  下部タブ・フッター・AppBar の的を44pxに、アプリ追加の案内は3ページ目から
- 新しいページ: 2体比較 /compare、更新履歴 /updates、ロール別 /heroes/role/[role]（6ロール）、用語集 /guide/glossary
- 横断検索でスキル名を引ける（索引 src/data/generated/skill_index.json、scripts/build_skill_index.mjs）
- Tier表とヒーロー詳細に前回の統計との差（hero_stats_camp_prev.json、sync_camp_tier.js が取り直しのたびに更新）
- 監査: 検査26（言語依存）・27（スキル索引）・28（更新履歴）・29（制御文字）、smoke はドイツ語ブラウザで回す
- 規約（AGENTS.md）: 書体、文字サイズ（読む文は12px以上）、出所表に前回の統計とスキル索引

## 2. これからの運用で気をつけること

- 掲載内容を変えて `npm run touch:updated` で日付を上げたら、`src/content/changelog.ts` にその日の行を足す（検査28が落とす）
- スキル名を直したら `node scripts/build_skill_index.mjs`（検査27が落とす）
- 統計を取り直すと、前回比は自動で「今回の1つ前」との差になる。調整前の注記（patchBasisHeroIds）を空にすると、その体にも差が出る
- 組み方を変えて Tailwind のクラスが消えたら、globals.css の @source inline に一定期間残す（2026-11 以降に消してよい分がある）
- 置換スクリプトで `\b` や `\r\n` を書くと、ツールを通るときに縮んで制御文字や改行が入る。こういう編集はエディタ（Edit）で行う

## 3. 残した課題（直していない。理由つき）

- 英語名が未確認の固有名詞: パッチの「王維」「百花繚乱」（英語ページに日本語のまま。smoke が毎回ここで落ちる）、
  比較ページのリソース名9種（闘志・訓戒・鋭気・狂気・質量・オーラ・怒気・熱量・電力充満。訳を作らず Other と表示）
- 用語集の定義文にある未確認の数値2つ（出典がリポジトリに無い。262ffae で入った）:
  Dive の「開始4分間はタワーに40%の軽減」、High Ground Tower の「周囲の敵ミニオンが3秒間75%減速」
- 共有プルダウンのボタンで、360px のとき長い選択肢が省略される（開いた一覧には全文）
- ヒーロー一覧の戦い方タイプの省略（390px で約40枚。前半は読める、全文は title）
- 装備シミュレータで、スマホの見た目の順と Tab の順がずれる（WCAG 2.4.3）
- ShareButton が 34px 角（共有部品）、/favicon.ico が 404（/icon.png は 200）
- next/image の priority は Next 16 で非推奨（ヒーロー一覧のカードなど、既存の箇所）
- 用語集への導線はガイドと検索からだけで、サイドバーとメニューには無い

## 4. 決めたこと（運営者の回答、2026-09-25）

- 日本語の本文は端末のフォント／スマホの作り直しは今の配色（白磁）のまま／AdSense の読み込みは今のまま
- ロール別ページは作るが、レーン別は作らない（/tier-list/[lane] と中身が重なり、検索で食い合うため）

## 5. 記録の置き場所

- レビュー指摘61件: `scratch/_review_0925.json`（`node scratch/_finding_0925.mjs <番号>` で読める）
- Workflow の結果: `scratch/wf_mobile_rework_0925_result.json`、`scratch/wf_features_0925_result.json`
- 計測: `scratch/mobile_0925/`（今）と `scratch/mobile_0925_before/`（9/25 朝）、スクリプトは `scratch/mobile_audit_0925.mjs` ほか
