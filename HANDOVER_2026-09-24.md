# 引き継ぎ 2026-09-24（S16「流の交わる地」の反映まわり）

次にこのリポジトリを触るエージェント向け。**作業を始める前に `AGENTS.md` と
`PATCH_NOTES_WORKFLOW.md` を読むこと。** ここに書くのは、その2つに載っていない
「いまどこまで進んでいて、次に何をするか」だけ。

---

## 0. 追記（同日夜、後続のセッション）

下の 2-2（シャドウアロー）と 2-4（新ヒーロー2体）は反映した。1〜6 は書かれた時点の記録として残す。

- **シャドウアロー** … `hok_items.json` に ID 1139 で追加。英語名は **Assassin's Wristbow**（英語ショップ 4735）。
  ゲーム内表示は回復「0.5秒ごと」、6%の前のアイコンは最大HPではなく失ったHPの形で、公式パッチノート原文の
  「失ったHP」に合わせた。**アイコンは公式から取れず暫定**（4646 の合成欄から切り抜き。上級の印が重なる）。
  CAMP の推奨ビルド欄（`suitStrategy`）が全ヒーローで空になり、`equipIcon` を拾う手段が無くなったため。
- **新ヒーロー2体** … 元流の子（アサシン）**583** / Flowborn (Assassin)、元流の子（サポート）**585** / Flowborn (Roamer)。
  heroId・英語名・アイコンは HoK Camp の `getherodataall` から。スキルは日本語 4597〜4612・英語 4725〜4734 を書き起こし、
  別エージェントが画面と照合。立ち回り・相性などの解説も同じく反証つきで書いた。
- **CAMP のランキングにまだ載っていない**ので、Tier表には入れていない（`data_freshness.json` の
  `campStats.unrankedHeroIds`）。`sync_camp_tier.js` はランキングに出たら自動で足して一覧から外す。
- **9月23日の調整対象8体**に「統計は調整前」の注記を出した（統計が 9/11 版のまま）。

### 新しく残ったこと

1. **HP回復の表示が S16 で変わった疑い。** 新ヒーローの画面は「1秒ごとのHP回復 10」「毎秒ごとのMP回復 3」で、
   既存（「1秒ごとのHP回復量」36〜88、MP14〜16）と行名も桁も違う。既存ヒーロー1体のステータス画面を撮れば決まる。
   決まるまで、新ヒーロー2体は `/heroes/stats` の順位表に入れていない（詳細ページの基本ステータスには出している）。
2. 元流の子（サポート）スキル3の表が画面下端で切れていて、下に行があるか未確認（4611 / 4734）。
3. ブラッドエッジ・永夜の守護の英語パッシブ（2-3 のまま）。
4. `npm run smoke` は `/en/patches` の「王維」「百花繚乱」で落ちる。どちらも英語名が未発表で、訳で埋めていないため。
5. 新ヒーロー2体は推奨ビルドが無い（CAMP が空、人気タブも未撮影）。ビルド欄とタイトルの「おすすめ装備」は自動で出ない。

---

## 1. いまの状態

- ブランチ `main`、作業ツリーはクリーン。**未プッシュのコミットは無い**（`60d2adb` が最新）。
- `src/data/data_freshness.json` … `site.lastUpdated` `staticData.items` `staticData.arcana` はすべて 2026-09-24。
- `skillData.pendingHeroIds` は **空配列**。スキル書き起こしの反映待ちは0体。
- 装備 114 点（シャドウアローは**未収録**）、ヒーロー 116 体（新ヒーロー2体は**未収録**）。
- `npm run audit` / `npx tsc --noEmit` / `npx eslint src` / `npm run build` はすべて通る。
  audit の既知の未解決は「クライアントJSONの import が20箇所」1件のみで、これはCIを止めない。

### 今回入れた5コミット（`029ba50..60d2adb`）

| コミット | 中身 |
|---|---|
| `c48c860` | S16 のスキル書き起こし5体（大喬191・蒼177・橘右京163・元流の子メイジ582・影558） |
| `af655ca` | アルカナ11種、フロレンティーノ631 / 元流の子タンク581・マークスマン584 のスキル |
| `9a752b5` | Amazon アソシエイトをサイトから全削除 |
| `dda475e` | 装備8点、582・558 の英語スキル、アルカナの回復の単位 |
| `60d2adb` | 582・558 の立ち回り解説を新仕様に書き直し |

---

## 2. 次にやること（優先順）

### 2-1. スマホ表示を作り直す ← **いま依頼されている最中で、未着手**

運営者から「スマホで見たときにかっこよくしてほしい」という依頼が出て、参考として
**MLBB.GG（`mlbb.gg/heroes`）のスマホ表示のスクリーンショット**が提示された。
提示された画面の要素は次のとおり。

- 暗い紺〜黒の地に、ロゴとハンバーガーだけのヘッダー
- 「Find counter picks for an enemy hero →」の大きな導線カード
- 検索ボックスと、並び替え・お気に入りのアイコンボタン
- レーン5種（Roam / Exp / Jungle / Mid / Gold）の横並びアイコンタブ
- 「133 heroes」の件数表示
- ヒーローは**2列のカード**。丸いアイコン＋名前＋ロールのバッジ＋LANES のアイコン

**ここで勝手に作り始めないこと。** 当サイトは `AGENTS.md` の「デザイン規約（玉璽）」で
配色・文字色の下限・金の使い方・重なり順まで決めてあり、**地は白磁 `#fbfaf7`、
`dark:` は使わない**（audit の検査16が `dark:` の再発を見張っている）。
MLBB.GG は真逆の暗色デザインなので、そのまま真似ると規約と正面衝突する。

先に運営者へ確認すべきこと。

1. 変えるのは**どのページか**（ヒーロー一覧だけか、サイト全体か）
2. 暗色に寄せたいのか、**いまの白磁のまま「情報の詰め方」だけ寄せたい**のか
3. 規約（`AGENTS.md` の玉璽の節）を書き換える話なのか、規約の範囲でやる話なのか

そのうえで、検査16のしきい値と `globals.css` を同時に直す。骨格を変えるときは
**`@source inline(...)` に消える utility を一定期間残す**こと（2026-09-23 に
ヒーロー詳細を1カラム化して、再訪者の画面が崩れた事故がある。`ac58fe9` を参照）。

### 2-2. シャドウアロー（新装備）を入れる ← **反映済み（0章）**

`src/data/hok_items.json` に**まだ無い**。日本語の内容は確定していて、
`src/data/patches.json` の `patch_9_23_system_3` に書いてある。

```
価格 2040G ／ 合成 天貫の弓＋サン・ストライク
+40 物理攻撃, +20% 攻撃速度, +20% クリティカル率, +650 最大HP
アクティブ-ステルス: ステルス状態（持続時間2.5秒、近距離戦時は半減）になり、
移動速度が30%増加し、0.5秒ごとに130〜260+ステルス状態前3秒間の最大HPの6%回復する
（スキル発動または通常攻撃でステルス状態を解除）（CD75秒）
```

（上はショップ画面の表記そのまま。公式パッチノートは「1秒ごと」「0.5秒後に解除」と
書いていて食い違うが、**装備データは実機の表示が正**。）

足りないのは2つだけ。

- **英語名**。公式の英語記事にも「the English name is not confirmed yet」と書かれている。
  ローマ字化や独自訳で埋めない（`AGENTS.md` 3章）。英語クライアントのショップ画面が要る。
- **アイコン**。`/images/items/{id}.webp` に置く。**IDからURLを組み立てないこと**
  （公式は `equipId` とファイル名が一致しない。`AGENTS.md` の画像の節）。

### 2-3. 英語のパッシブ説明が2件ぶん古い

`npm run diff:items` が毎回この2件を出す。数値は入れたが、**説明文は英語画面が無いと書けない**。

- ブラッドエッジ（1132）… パッシブ「充溢」が追加されたのに `passive_en` は旧文のまま
- 永夜の守護（1347）… パッシブ「守護」の条件と効果が変わったのに `passive_en` は**旧仕様のまま（誤り）**

運営者に「英語クライアントのショップでこの2つを選んだ状態」を2枚頼んである。

### 2-4. 新ヒーロー2体 ← **反映済み（0章）**

サイトに1行も無い。**素材はもう撮影済み**。

| | 日本語 | 英語 |
|---|---|---|
| 元流の子（アサシン）「叡智の双爪」 | スクショ 4598〜4615 | 4725〜4737 に含まれる |
| 元流の子（サポート）「久遠の調べ」 | 同上 | 同上 |

英語名は **Flowborn (Assassin)**「Vision Unbound」、**Flowborn (Roamer)**「Steadfast Oath」。
（サポートではなく **Roamer** 表記だった。ショットリストの「Flowborn (Support)」は誤り。）

要るのは、ヒーロー項目・ロール・難易度・基本ステータス・スキル4件・アイコン。
**アイコンが公式の一覧にまだ無い**ので、そこだけ後回しになる可能性がある。

### 2-5. コード入力（CDKEY）のページ

運営者と方針だけ決めて未着手。2本立て。

1. コードの入れ方を説明するページ（ゲーム内「コミュニティ → CDKEY」）
2. **公式が出したコードだけ**を並べる一覧（出典URL・掲載日・期限・確認日を各行に持たせる）

まとめサイトから拾ったコードは載せない。

---

## 3. このリポジトリで踏みやすい地雷（今回実際に踏んだもの）

### 3-1. 装備・アルカナの数値を変えたら、ビルド解説の合計値が古くなる

`src/content/buildNotes.ts` は227本の解説の本文に**装備＋アルカナの合計値を直書き**している。
装備を1つ直すと、その装備を含む全ビルドの合計が変わる。今回、装備8点とアルカナ11種の変更で
**227本中126本の合計が動き、本文の約290箇所が古くなった**。

しかも**前日のアルカナ変更（`af655ca`）のぶんが直っていなかった**。
データだけ直して解説を放置すると、こうして静かに溜まる。

検出と修正に使ったスクリプトを `scratch/` に残してある。**消さないこと。**

| ファイル | 役割 |
|---|---|
| `scratch/_find_stale_totals.mjs` | 「+旧値」の形で本文に残った合計値を出す |
| `scratch/_find_stale_loose.mjs` | 「+」が付かない書き方（`45高い309` / `230 physical defense`）を出す |
| `scratch/_find_stale_diffs.mjs` | ビルド1と2の「差」の記述が古くなった候補を出す |
| `scratch/_fix_stale_totals.mjs` | 上を機械的に置換する（本文に1回だけ出る場合のみ） |
| `scratch/_verify_totals.mjs` | 本文の数値を、いまのデータから計算した合計と突き合わせる |
| `scratch/_check_ja_en_numbers.mjs` | 同じビルドの日英で数値が食い違っていないか見る |

使い方は、比較したい時点のデータを `git show <ref>:src/data/hok_items.json > scratch/_old_items.json`
のように取り出してから `_find_stale_totals.mjs` → `_fix_stale_totals.mjs` の順。
**最後に必ず `_check_ja_en_numbers.mjs` を通すこと。** 今回、機械置換が1件だけ
別の値に書き換えてしまい（556の英語の最大HPが `+6,837` → `+5,837`）、この検査で見つかった。

これは検査にできる性質のものなので、**`scripts/audit.mjs` に組み込むのが本筋**。
まだやっていない。

### 3-2. 書き起こしは実機から。英語の散文は英語画面が要る

- 数値は日本語画面から英語側へ入れてよい（`PATCH_NOTES_WORKFLOW.md` 8章）。
- **説明文は駄目。** 英語の文章は英語クライアントの画面が要る。
- 固有名詞を訳で埋めない。今回、582 のスキル2の英語名を日本語名から訳して
  `Meteor Bloom` と置いてしまい、実機は `Starflow Cascade` だった（`dda475e` で修正）。

### 3-3. ゲームの表示が矛盾していても、画面どおりに書く

- 元流の子（マークスマン）584 のパッシブは、説明文が「移動速度が12.5%増加」なのに
  同じ画面の表が 3.5%〜7% のまま。**直さずに画面どおり**入れてある。
- 影 558 の英語画面は、本文の主語がヒーロー名 `Umbrosa` ではなく二つ名 `Nightplume`。
  ここだけサイトの表記（Umbrosa）に揃えた。数値と仕様は触っていない。

### 3-4. 回復の単位は画面によって違う

- ヒーローのステータス画面 … 「1秒ごとのHP回復量」
- アルカナ画面 … 「5秒ごとのHP回復」

**単位が違うので足せない。** アルカナ計算機は基礎値への加算をやめ、アルカナぶんの合計だけを出す
（`src/lib/arcanaStats.ts` の `healthRegen` から `baseStatKey` を外してある）。

### 3-5. プッシュ前

`npm run touch:updated` で `site.lastUpdated` を当日に。`npm run audit` が上げ忘れを落とす。
ガイド3本と各ページは別の日付を持つ（`pages.*.updatedAt`）。

---

## 4. スクリーンショットの置き場所

`C:\Users\81901\Desktop\Screenshots\スクリーンショット (NNNN).png`

今回のぶんの対応。

| 番号 | 中身 |
|---|---|
| 4590〜4612 | 日本語のヒーロー画面（大喬・ルアンナ・新ヒーロー2体） |
| 4598〜4615 | 新ヒーロー2体のスキル（日本語） |
| 4613〜4710 | **装備ショップ**。この範囲に全装備が入っている |
| 4711〜4715 | アルカナ画面（Lv.5） |
| 4725〜4737 | 新ヒーロー2体のスキル（英語） |
| 4738〜4742 | 影 558 のスキル（英語） |
| 4743〜4746 | 元流の子（メイジ）582 のスキル（英語） |

**1445〜1658 はワイリフの画像なので無視すること。**

### 大量のスクショから目当ての画面を探す方法

1枚ずつ開くと高くつく。`sharp`（導入済み）で**同じ位置を切り出して並べた一覧画像**を作り、
それを1枚読むほうが速い。今回は装備ショップ98枚から9点を特定するのに使った。

```js
// アイテム名の帯だけを切り出して 7列で並べる
const buf = await sharp(file).extract({ left: 1495, top: 203, width: 360, height: 72 }).toBuffer();
```

詳細パネルは `left: 1490, top: 200, width: 380, height: 650` で読める。
一覧（価格が見える側）は `left: 335, top: 350, width: 840, height: 430`。
どれも 1920x1080 のスクショでの座標。

---

## 5. 運営者との約束ごと（セッションをまたいでも有効）

- **`C:\Users\81901\Desktop\ワイリフサイト` は読むことも編集することも禁止。** 別担当。
- ポートは HoK が **3211**、ポータルが 3212。3000 / 3100 はワイリフ。
  （2026-09-24 時点で 3211 が応答しないまま占有されていた。別ポートを使う場合は 3213 以降。）
- **ゴッドタブのデータは公開しない**（他プレイヤーのハンドルが写るため）。
- **本番を高頻度でポーリングしない。**
- **データ収集方法（スクショ → AI で書き起こし）をサイトに公開しない。**
- **ゲームのスクリーンショットを証拠としてサイトに載せない**（EULA 7(e)）。
- **`public/sw.js` の `CACHE_NAME = 'hok-hub-cache-v5'` は絶対に上げない。**
- **`scratch/` は消さない。**
- `skills/ja.json` の `passive` / `skill1-4` の `description` `name` `status_text` `stats`
  `cooldown_text` は書き起こしなので、**日本語の自然さのために直さない**。
- `git push` とデプロイは明示の指示があるときだけ。コミットは区切りごとに行ってよい。
- サブエージェントは親のモデルを継承させる（`model` を指定しない）。
  **4本以上回す前に、本数・目的・モデルを出して承認を取る。**
- ポータル（`C:\Users\81901\Desktop\hub-game-portal`）は別プロジェクトだが読み書き許可済み。

---

## 6. 運営者に頼んである撮影

- 英語クライアントのショップで **ブラッドエッジ** と **永夜の守護** を選んだ状態（各1枚）
- **シャドウアロー**の英語画面（英語名の確認用）
