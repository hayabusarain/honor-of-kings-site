# 引き継ぎ 2026-09-26（夜の配色「墨」への作り直し）— 完了

MLBB Hub の申し送り（docs/handoff-from-mlbb-2026-09-24.md）に従って、HoK Hub の見た目を夜の配色に作り直した。
運営者の決定（2026-09-26）: 地の色は「墨」（墨・漆・青墨の3案から）、文字は14px以上（ふりがなだけ例外）、仕上げたら確認なしで main に入れて push してよい。

## 1. 入ったもの

- 配色: globals.css の @theme で色の変数を段ごとに写し替え（部品のクラス名は変えない）。値は scratch/dark_palette_hok_0926.mjs が生成・検算
- 全ページ: 文字14px以上、選択中は金の線（src/components/common/tones.ts の SELECTED）、暗幕は黒、冒頭の帯（.page-hero）と節見出し（.section-title）、
  ロール・レーンの図柄（src/components/icons/GameIcons.tsx）。8班×実装・検証の Workflow（hok-dark-rollout-0926）で仕上げた
- theme-color・manifest・OGP 画像を墨に
- 監査の検査16: 14px 未満の文字（上限0、ふりがな例外）と、墨の塗りの選択中を止める
- AGENTS.md のデザイン規約を夜の配色で書き直した（地・文字色の比の表・壊れるもの・金・文字サイズ）

## 2. 確かめたこと（手元の本番ビルド）

- 型・lint（エラー0）・監査・共通ルール・ビルド（660ページ）
- スマホ全24ページ（390・360、日英）: 14px 未満はふりがなだけ、横はみ出し0、エラー0
- 描画後のコントラスト（scratch/contrast_render_0926.mjs）: 390px の30ページで不足0。1280px の1件は無効化中のボタン（WCAG の対象外）
- ハイドレーション: 8ページ×3言語でエラー0。smoke は既知の /en/patches（王維・百花繚乱）だけ

## 3. 残した課題

- ヒーロー詳細のおすすめビルドが1件1行になり、ページが縦に長くなった（390px で ja 14.1→16.0画面）。14px で語の途中で折らない組み方がこれしかなかった
- 語の途中で折らない対策の一部は [word-break:auto-phrase] に頼っている（Chrome の機能。iOS Safari は未確認）
- 本文のカタカナ語が行の途中で折れる（日本語の通常の組版。line-break: strict などを globals.css に当てるかは未判断）
- 装備シミュレータのスマホの6枠の帯は枠33px・「すべて外す」36pxで 44px に届かない
- Tier表の共有用表示で「フロレン／ティーノ」「（マーク／スマン）」の2語だけ折れる（1マスに8字が入らない）
- 採用率の棒グラフが金の塗り（bg-brand-500）のまま。データの棒で、選択中でも序列でもない
- 検索でヒーローの補足のロールが英語のまま（元から）、iOS の追加手順のモーダルに role="dialog" が無い（元から）
- 作業中、開発サーバーの .next/dev/prerender-manifest.json が同時書き込みで壊れ、17:00〜17:12 に全ページ 500 になった。再起動で直る
- 各班の報告の全文: scratch/wf_dark_rollout_0926_result.json
